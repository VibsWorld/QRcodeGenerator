# QR Code Generator

Run the following docker to test it locally. 
```
docker run --rm -p 8080:8080 vibs2006/qr_code_generator
```
Navigate to <http://localhost:8080/index.html> after running above docker image. 
